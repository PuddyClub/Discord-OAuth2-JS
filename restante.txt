A gente vai pegar esse valor e colocar a parte fundamental dele dentro do discordSession.firebase.setAccount(user) para validar quais são os dados novos
console.log(userFiredata);

Fazer o discordSession.firebase.setAccount atualizar os dados do usuário na cache (result.updated and result.data)


checar discordSession.firebase.get



O redirect final com firebase manda para uma página para logar o javascript do firebase antes de ir para a página redirecionada




Se detecta que não tem Discord, deslogar do Firebase
se detectar que não tem firebase, deslogar do Discord





Fazer o leitor de interaction